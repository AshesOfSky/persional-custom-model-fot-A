# 数据源扩展说明

## 新增数据支持

本次更新整合了 **akshare** 数据源，大幅扩展了可分析标的的范围。

### 支持的市场和代码格式

| 类型 | 代码格式 | 示例 | 数据源 |
|------|---------|------|--------|
| A股个股 | `XXXXXX.SS` 或 `XXXXXX.SZ` | `600519.SS` (茅台) | akshare / yfinance |
| A股 ETF | 直接输入6位代码 | `518880` (黄金ETF) | akshare |
| 港股 | `XXXX.HK` | `0700.HK` (腾讯) | akshare / yfinance |
| 美股 | 直接输入代码 | `AAPL` (苹果) | yfinance / akshare |
| 国内期货 | 主力连续代码 | `AU0` 或 `GCMAIN` (黄金) | akshare |
| 外汇 | 标准代码 | `XAGUSD` (白银美元) | akshare |

### 期货代码映射

**金属**
| 用户输入 | 实际代码 | 品种 |
|---------|---------|------|
| `GCMAIN` | `AU0` | 黄金主力连续 |
| `AU0` | `AU0` | 黄金主力连续 |
| `AG0` | `AG0` | 白银主力连续 |
| `XAGUSD` | `AG0` | 白银主力连续（映射）|
| `CU0` | `CU0` | 铜主力连续 |
| `AL0` | `AL0` | 铝主力连续 |
| `ZN0` | `ZN0` | 锌主力连续 |
| `NI0` | `NI0` | 镍主力连续 |

**黑色系**
| 用户输入 | 实际代码 | 品种 |
|---------|---------|------|
| `SLMAIN` | `RB0` | 螺纹钢主力连续 |
| `RB0` | `RB0` | 螺纹钢主力连续 |
| `HC0` | `HC0` | 热卷主力连续 |
| `I0` | `I0` | 铁矿石主力连续 |
| `J0` | `J0` | 焦炭主力连续 |
| `JM0` | `JM0` | 焦煤主力连续 |

**能源化工**
| 用户输入 | 实际代码 | 品种 |
|---------|---------|------|
| `CL0` | `SC0` | 原油主力连续 |
| `SC0` | `SC0` | 上海原油主力连续 |

**农产品**
| 用户输入 | 实际代码 | 品种 |
|---------|---------|------|
| `M0` | `M0` | 豆粕主力连续 |
| `RM0` | `RM0` | 菜粕主力连续 |
| `C0` | `C0` | 玉米主力连续 |
| `CF0` | `CF0` | 棉花主力连续 |
| `SR0` | `SR0` | 白糖主力连续 |

### A股 ETF 示例

| 代码 | 名称 |
|------|------|
| `518880` | 黄金ETF |
| `513050` | 中概互联ETF |
| `510300` | 沪深300ETF |
| `510050` | 上证50ETF |
| `159915` | 创业板ETF |

## 安装依赖

```bash
pip install -r requirements.txt
```

新增的主要依赖：`akshare>=1.15.0`

## 技术架构

```
app.py
  └── data_router.py (智能路由)
        ├── data_fetcher.py (yfinance 数据源)
        └── data_fetcher_akshare.py (akshare 数据源)
```

### 数据路由逻辑

1. **代码类型识别**：根据代码格式和映射表判断资产类型
2. **数据源优先级**：
   - A股个股：优先 akshare，失败时使用 yfinance
   - A股 ETF：仅 akshare（yfinance 不支持）
   - 国内期货：仅 akshare
   - 外汇：仅 akshare
   - 美股：优先 yfinance，失败时使用 akshare
   - 港股：优先 akshare，失败时使用 yfinance

3. **自动回退**：首选数据源失败时，自动尝试备选数据源

## 界面改进

- 输入框提示已更新，展示支持的代码类型
- 数据看板显示实际使用的数据源（`yfinance` 或 `akshare`）
- 错误提示更加智能，针对不同情况给出针对性建议

## 故障排查

### 如果 akshare 获取失败

1. 检查网络连接（akshare 需要访问国内财经网站）
2. 更新 akshare：`pip install -U akshare`
3. 查看具体错误日志

### 如果 yfinance 获取失败

1. 检查是否能访问 Yahoo Finance
2. 代码格式是否正确（美股无需后缀）
3. 该标的是否已退市或更名
