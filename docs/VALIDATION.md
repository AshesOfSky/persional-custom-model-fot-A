# 独立行情终端验证记录

验证日期：2026-09-10。

## 范围

独立保留行情页、证券搜索、自选配置、原生报价与手动刷新、指标、信号置信度、技术叠加、基本面摘要、PDF 导出。后台监控入口及其他业务页面已移除。

原始计算模块共 18 个按文本逐一与来源核对一致，包括指标、趋势分析、复合买卖信号、信号置信度、买入操作计划、支撑压力、江恩、艾略特波浪、形态、量价与风险模块。原因子选股文件仅保留分析函数引用的北向资金辅助函数；未携带全市场选股扫描功能。技术结构适配、行情质量和实时信号服务继续使用原实现。

## 已完成检查

- 119 项原有相关后端测试通过，覆盖实时信号与完整盘中预览、连续置信度、行情/分析视图、数据质量、技术结构可靠性、自选服务与存储。
- 2 项独立版本测试通过：空白自选起步，以及不暴露监控、回测、组合、选股、板块蒸馏等路由。
- TypeScript 检查和 Vite 生产构建通过，ESLint 通过。构建仍提示单个图表主包超过 500 kB，该提示不影响构建结果。
- 新接口类型从独立后端 OpenAPI 生成，未沿用整套研究系统的旧接口表。
- 在隔离端口、空白浏览器上下文和独立运行目录，以公共示例证券 `000001.SZ` 完成真实行情冒烟验证。
- 行情、搜索、报价、基本面、研究分析与四类技术叠加请求返回 HTTP 200；页面存在 15 个图表画布，无浏览器脚本错误。
- 午后临时日 K 与预估信号可见，正式分析仍使用完成日 K。页面没有后台监控入口，也没有指向已移除页面的详情链接。
- 行情页导出产生可打开的一页横向 A4 PDF；已查看截图和 PDF 首页面，图表与数据来源说明完整。

浏览器实测使用 Windows 上已安装的 Edge。测试截图、PDF 和行情缓存只保存在被忽略的本地目录，不上传。

## 隐私处理

从允许的源码文件建立独立目录；没有复制原项目的 `.git`、自选名单、持仓、交易记录、数据库、缓存、日志、截图、个人文档、密钥文件或本机配置。公共交易日历与原始公告是本次从交易所重新捕获的资料。

初始自选为空。默认证券仅为公共示例。`.env.example` 的密钥为空，真实 `.env`、运行数据与导出制品由 `.gitignore` 排除。

上传前对暂存文件检查本机用户名与绝对路径、常见凭据格式，以及本机已配置的数据源密钥的精确值；提交身份使用 GitHub noreply 邮箱。

## 验证边界

此次真实行情验证在本机已有数据源权限下完成，不代表无密钥、每个证券、每种周期或其他操作系统均经过真实行情验证。无密钥时使用公开适配器，手动 AICubes 强制刷新会报告未配置。原测试中仅用于比较独立 S1 策略与行情信号函数身份的那一项不属于本版本测试范围，因为 S1 回测策略未纳入此行情终端。

没有运行后台监控，没有重启原系统，也没有执行交易或发送外部通知。仓库更新替换 `main` 的当前文件树，不重写旧提交历史。
